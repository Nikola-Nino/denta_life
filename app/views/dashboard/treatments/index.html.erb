<section class="text-gray-600 body-font">
  <div class="container px-5 py-24 mx-auto">
    <div class="flex flex-wrap w-full mb-20">
      <div class="lg:w-1/2 w-full mb-6 lg:mb-0">
        <h1 class="sm:text-3xl text-2xl font-medium title-font mb-2 text-gray-900"><%= @treatments.empty? ? 'No Posts are created yet' : 'Your Treatments ' %></h1>
        <div class="h-1 w-20 bg-indigo-500 rounded"></div>
      </div>
    </div>
    <div class="flex flex-wrap -m-4">
    <% @treatments.each do |treatment| %>
      <div class="xl:w-1/3 md:w-1/2 w-full p-4 ">
        <div class="bg-gray-100 p-6 rounded-lg ">
          <h3 class="tracking-widest text-indigo-500 text-xs font-medium title-font"><%= treatment.full_name %></h3>
          <h2 class="text-lg text-gray-900 font-medium title-font mb-4"><%= treatment.diagnosis %></h2>
          <p class="leading-relaxed text-base" style="margin-bottom: 20px;"><%= treatment.date_of_visit %></p>

        </div>
      </div>
      <% end %>
    </div>
  </div>
</section>